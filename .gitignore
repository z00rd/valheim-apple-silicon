# Secrets and local data — NEVER commit
.env
backups/
.caffeinate.pid
.udp-bridge.pids
*.log

# Local artifacts (bridge PID files, working compose copies, Python cache)
*.bak
__pycache__/
*.pyc

# Personal debugging journal (contains private IPs/dates). Lessons live in TROUBLESHOOTING.md.
SETUP-LOG.md

# macOS
.DS_Store

# IDE
.idea/
.vscode/

# Personal decision journal (private IPs/names) — local only
ROADMAP.md
